{{ form_start(form) }}
    <div class="row">
        <div class="col-6">{{ form_row(form.titre) }}</div>
        <div class="col-6">{{ form_row(form.photo) }}</div>
    </div>
    {{ form_row(form.commentaire) }}
    <button type="submit" class="btn btn-primary" data-turbo-frame="_top">
        {{ button_label|default('Poster votre commentaire') }}</button>
{{ form_end(form) }}
